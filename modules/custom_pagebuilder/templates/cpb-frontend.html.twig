<div class="{{ row_class }}" style="{{ row_style }}" {{ row_animate.duration }} {{ row_animate.delay }} >
  <div class="cpb-inner {{ row.attr.style_space }}">
    <div class="cpb-container {{ row.attr.layout }}">
      <div class="row-wrapper clearfix">
        <div class="row">
          {% for col in columns  %}
              {{ col }}
          {% endfor %}
        </div>
      </div>
    </div>
  </div>
  {% if row.attr.bg_attachment and row.attr.bg_attachment ==  'fixed' %}
      <div class="cpb-parallax-inner skrollable skrollable-between" data-bottom-top="top: -50%;" data-top-bottom="top: 0%;"></div>
  {% endif %}
</div>